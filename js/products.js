// Pavia product catalog — intentionally EMPTY.
// The live catalog comes from Firebase (seeded in the admin Studio). This list
// used to hold demo products, but anything here is shown — and orderable — by
// real customers whenever the backend is unreachable with no cached catalog,
// so it must stay empty. With no data the storefront shows its branded
// "collection coming soon / taking a short pause" states instead.
window.PAVIA_DEFAULT_PRODUCTS = [];
